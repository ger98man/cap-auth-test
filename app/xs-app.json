{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/(.*)",
            "destination": "srv-api",
            "authenticationType": "xsuaa"
        },
        {
            "source": "^/(.*)",
            "destination": "srv-api",
            "authenticationType": "xsuaa"
        }
    ]
}